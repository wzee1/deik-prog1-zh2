#ifndef header
#define header

#include <iostream>
#include <fstream>
#include <vector>
#include <sstream>

std::vector<std::vector<std::string>> read_file(const std::string &filename, char delimiter);

#endif